import React from "react"
import { Link } from "gatsby"

const Navigation = ({ menu }) => {
  // console.log(menu)
  // ;<div className="menus">
  //   <ul>{menu}</ul>
  // </div>
}

export default Navigation
